#[cfg(test)]
mod tests {
    use kolkulator::short::addition;

    // I don't have a cuue how to implement a unit test for a program that takes input from keybaord and output to stdout
    #[test]
    fn test_addition() {
        addition();
    }
}
